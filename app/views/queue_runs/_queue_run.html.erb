<article id="<%= dom_id(queue_run) %>" class="rounded-2xl border border-white/10 bg-slate-900 p-4">
  <div class="flex items-start justify-between gap-4">
    <div>
      <p class="text-sm text-slate-400"><%= queue_run.created_at.strftime("%Y-%m-%d %H:%M:%S") %></p>
      <p class="mt-2 text-sm font-medium text-slate-100"><%= queue_run.input %></p>
      <% if queue_run.output.present? %>
        <p class="mt-2 text-sm text-slate-400"><%= queue_run.output %></p>
      <% end %>
    </div>
    <span class="<%= queue_status_classes(queue_run) %>"><%= queue_run.status %></span>
  </div>
</article>
